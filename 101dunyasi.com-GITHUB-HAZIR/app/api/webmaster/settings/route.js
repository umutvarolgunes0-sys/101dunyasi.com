import {NextResponse} from 'next/server';import {getUserFromCookies} from '@/lib/auth';import {readData,writeData} from '@/lib/store';
export async function PATCH(req){const u=getUserFromCookies(req.cookies);if(!u||u.role!=='WEBMASTER')return NextResponse.json({error:'Webmaster yetkisi gerekli.'},{status:403});const b=await req.json(),d=readData();for(const k of ['stationName','welcome','siteNotice'])if(b[k]!==undefined)d.settings[k]=String(b[k]).slice(0,300);for(const k of ['autoFallback','requestEnabled','registrationEnabled','maintenance'])if(typeof b[k]==='boolean')d.settings[k]=b[k];if(Number.isFinite(Number(b.maxUploadMb)))d.settings.maxUploadMb=Math.max(5,Math.min(200,Number(b.maxUploadMb)));d.auditLogs.push({id:Date.now(),actor:u.username,action:'settings_update',at:new Date().toISOString()});writeData(d);return NextResponse.json({settings:d.settings})}
