import {NextResponse} from 'next/server';import {getUserFromCookies} from '@/lib/auth';import {readData,writeData} from '@/lib/store';
export async function PATCH(req){const u=getUserFromCookies(req.cookies);if(!u||!['ADMIN','WEBMASTER'].includes(u.role))return NextResponse.json({error:'Admin yetkisi gerekli.'},{status:403});const b=await req.json(),d=readData();if(typeof b.autoFallback==='boolean')d.settings.autoFallback=b.autoFallback;if(typeof b.requestEnabled==='boolean')d.settings.requestEnabled=b.requestEnabled;if(b.stationName)d.settings.stationName=String(b.stationName).slice(0,80);writeData(d);return NextResponse.json({settings:d.settings})}
