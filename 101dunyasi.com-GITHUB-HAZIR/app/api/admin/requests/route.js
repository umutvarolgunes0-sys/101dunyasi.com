import {NextResponse} from 'next/server';import {getUserFromCookies} from '@/lib/auth';import {readData,writeData} from '@/lib/store';
export async function PATCH(req){const u=getUserFromCookies(req.cookies);if(!u||u.role!=='ADMIN')return NextResponse.json({error:'Admin yetkisi gerekli.'},{status:403});const {id,status}=await req.json();if(!['pending','approved','rejected','played'].includes(status))return NextResponse.json({error:'Geçersiz durum.'},{status:400});const d=readData(),r=d.requests.find(x=>x.id===String(id));if(!r)return NextResponse.json({error:'İstek bulunamadı.'},{status:404});r.status=status;writeData(d);return NextResponse.json({request:r})}
